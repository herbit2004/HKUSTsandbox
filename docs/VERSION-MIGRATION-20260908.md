# 2026-09-08 版本迁移核查

本次将项目源代码、全部已保存的运行材料、楼宇/室内索引及来源/验收附件移入 `versions/2026-09-08-campus/`；独立复制为 `versions/2025-03-27-government-source/`，该副本实际渲染未修补的政府源网格。

- 迁移前后逐文件 SHA-256 校验：9,155 个原有文件，3,226,054,625 bytes；原有运行资产及 source-* 材料均保留原字节。操作系统元数据和可重建缓存不计入材料。
- 两副本中检查了 9,158 对对应文件的 inode：彼此独立，没有通过素材链接共用文件。
- 独立保留完整 entity-registry、entity-resources、室内 manifest、原始室内数据、照片与源材料。
- 原始摄影 GLB 292/292 与源 manifest 的大小及 SHA 匹配，对应 b3dm 292/292 存在并匹配；原始 GLB 全部自包含。3 个后续删除悬片的瓦片恢复到未修补的原始文件。
- 原始源副本的后续地形、摄影细节、单体外观、纹理替换及 current-form 流均关闭；相关后加资料保留自身日期，仅作独立注释，不作为源底图渲染。
- 两副本分别通过 TypeScript 和静态构建。原始源副本在独立 4330 服务实际显示原始校园模型、完整实体列表和 2026 注释说明。
- 根目录 `npm run build` 正确选择 latest，原子更新 4317 至 `20260908T092858356771Z`。浏览器刷新后完整版地图、实体列表及创科楼点选/贴图可见。
- 版本复制测试验证：更改新副本不改变父副本；重名和素材链接被拒绝；失败时 latest 不变。
- R2 Worker 测试覆盖原始字节、Range、HEAD、ETag、全景 allowlist、导入认证、校验失败和原子激活。

封存时最新副本有 9,157 个材料文件，原始源副本有 9,158 个；各自清单位于本机 `INVENTORY.local.json`。原有 `publishedRuntimeAcceptance` 与逐栋证据保留，未把已有全校质量目标改为通过。

原始源副本不是 2025 年当时的完整应用快照：应用和实体资料在 2026-09 整理。按需官方全景仍依赖远端服务；本机已保存的图片和节点索引各自归入副本，未声称归档了所有尚未下载的网络内容。部分早期临时源输入已丢失，仍按各版 DATA.md 报明。

完整本机核验回执在 `.local/version-*-validation.json`，包含材料校验详情；大资产、图片和这些私有清单不进入公共 GitHub。
