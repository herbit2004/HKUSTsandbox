# 迁移验证记录 · 2026-09-08

- 独立仓库根 `/Users/herbit/Desktop/code/HKUSTsandbox`；GitHub `herbit2004/HKUSTsandbox` 实测 `isPrivate=false`、默认 `main`。未继承父仓库历史。
- 原始复制逐文件对照：150,231 文件、46,573,732,706 字节；软链接目标一致。原证据清单在 `.local/migration/full-copy-verification.json`；18个迁移前被编辑文件的原本及原dist保留在同目录备份中。无原运行资产丢失。
- 新根 full-local 类型检查与静态构建通过。固定4317的16项首页/脚本/数据/地形/模型资源HTTP200且与新dist字节一致；6项旧哈希脚本在原子切换后仍可用。进程cwd实测为新根。
- 对真实GitHub URL进行全新 clone，执行README的Release下载校验、`npm ci`、类型检查和构建，全部通过。公开包107,003,483字节，SHA-256 `aaa366e506c35d3206ff34bb3124a607c762466c54ba173ae5a1cbffa6e05814`；319个内容文件哈希及包内清单通过，包含292个当前政府瓦片和3项既有源三角修正。
- 复用同一个Chrome标签先看新根4317，再看临时4318干净clone，均看到真实校园地形/建筑纹理；公开数据限制与署名明确显示。最后回到4317完整数据并关闭4318。没有新增重复预览标签。
- 数据安装工具验证了拒绝覆盖已有数据、校验损坏文件；验收Markdown生成验证持久化运行说明，保留54个历史验收行。最终通过仍0栋，canonical分母仍未确定。
- 旧目录已替换为指向新根的兼容软链接；今后只在新根迭代。源码根、公开数据获取及本地运行均不依赖旧实体目录。

这些验证覆盖工程迁移与可运行性，不覆盖全校逐栋视觉质量，也不证明缺失的35个历史临时源目录能够重放。完整本机模型与校方资料已保全，公开包的有限范围在README和页面中明确说明。

安装期审计的已知未完成项：11个受影响依赖包（8 high / 2 moderate / 1 low）尚未升级，详见DEPLOYMENT.md。另一次在线审计计划请求被自动审批拒绝，未重试；统计依据本次npm ci已经返回并缓存的官方响应，不声称另行在线复核或已取得完整自动修复方案。
